#include "holberton.h"
/**
  *-isupper  - function that checks for uppercase character 
  *variable type int
  *Return 1 for uppwercase and 0 otherwise
  */
	int _isupper(int c);
{
	if (c >=65 && <= 90 )
	{
	return (1);
	}
	else 
	{
	return (0);
		}
}
    
